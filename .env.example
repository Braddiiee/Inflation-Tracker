# Copy to `.env` for local overrides (post-MVP / cloud migration).
# MVP uses SQLite only; these are placeholders for future hosting.

# DATABASE_URL=sqlite:///data/tracker.db
# DATABASE_URL=postgresql://user:pass@host:5432/tracker
