"""
Reusable Streamlit UI building blocks.

Responsibilities (future):
- Shared sidebar (global date range, store, category filters)
- Standard form sections and validation feedback (st.error / st.success)
- Chart layout wrappers for consistent dashboard styling

Prevents duplication across `pages/home.py`, `pages/entry.py`, and `pages/analytics.py`.
"""
